{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/todo-api/v1/post-todo-response.schema.json",
  "title": "POST /api/v1/todo – Response",
  "description": "Response after creating a new ToDo item.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Indicates whether the operation was successful."
    },
    "id": {
      "type": "integer",
      "description": "The unique identifier of the created ToDo item."
    },
    "validationErrors": {
      "type": [
        "array",
        "null"
      ],
      "description": "List of validation errors if any occurred during creation.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "success",
    "id"
  ]
}