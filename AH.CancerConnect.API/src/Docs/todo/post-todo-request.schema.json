{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/todo-api/v1/post-todo-request.schema.json",
  "title": "POST /api/v1/todo – Request",
  "description": "Create a new ToDo item for a patient.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "patientId": {
      "type": "integer",
      "minimum": 1,
      "description": "Patient identifier who owns this ToDo item."
    },
    "title": {
      "type": [
        "string",
        "null"
      ],
      "maxLength": 250,
      "description": "Optional title for the ToDo item."
    },
    "detail": {
      "type": "string",
      "maxLength": 1000,
      "description": "Detailed description of the ToDo task."
    },
    "date": {
      "type": [
        "string",
        "null"
      ],
      "format": "date-time",
      "description": "Optional due date for the ToDo item (ISO 8601 format)."
    },
    "time": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^([01]?[0-9]|2[0-3]):[0-5][0-9]:[0-5][0-9]$",
      "description": "Optional time for the ToDo item (HH:MM:SS format)."
    },
    "alert": {
      "type": [
        "string",
        "null"
      ],
      "enum": [
        "OnDay",
        "EveningBefore",
        null
      ],
      "description": "Optional alert type for the ToDo item."
    }
  },
  "required": [
    "patientId",
    "detail"
  ]
}