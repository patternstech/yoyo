{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/notes-api/v1/put-notes-request.schema.json",
  "title": "PUT /api/v1/notes – Request",
  "description": "Update an existing note for a patient.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 1,
      "description": "The unique identifier of the note to update."
    },
    "patientId": {
      "type": "integer",
      "minimum": 1,
      "description": "Patient identifier who owns this note."
    },
    "title": {
      "type": [
        "string",
        "null"
      ],
      "maxLength": 250,
      "description": "Optional title for the note."
    },
    "noteText": {
      "type": "string",
      "maxLength": 1000,
      "description": "The main content of the note."
    },
    "recordingPath": {
      "type": [
        "string",
        "null"
      ],
      "maxLength": 500,
      "description": "Optional path to an associated audio recording."
    }
  },
  "required": [
    "id",
    "patientId",
    "noteText"
  ]
}